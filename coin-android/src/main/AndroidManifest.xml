<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mlucky.coin.app.gui" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_dollar"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ApplicationActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".TransactionListActivity"
                android:screenOrientation="portrait"/>
        <activity
                android:name=".TransactionActivity"
                android:screenOrientation="portrait"/>
        <receiver android:name="com.mlucky.coin.app.broadcast.SmsReceiver" android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.DATA_SMS_RECEIVED_ACTION"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>
